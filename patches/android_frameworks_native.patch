diff --git a/services/sensorservice/SensorDevice.cpp b/services/sensorservice/SensorDevice.cpp
index 115a983bc..dfd482c80 100644
--- a/services/sensorservice/SensorDevice.cpp
+++ b/services/sensorservice/SensorDevice.cpp
@@ -672,8 +672,13 @@ void SensorDevice::convertToSensorEvents(
 }
 
 void SensorDevice::handleHidlDeath(const std::string & detail) {
-    // restart is the only option at present.
-    LOG_ALWAYS_FATAL("Abort due to ISensors hidl service failure, detail: %s.", detail.c_str());
+    // nicki (ported from 15.1): the legacy Sony/Bosch sensors HAL can crash
+    // during HIDL calls. Upstream LOG_ALWAYS_FATAL here takes system_server
+    // down with it (boot loop). Don't abort — log and carry on with no working
+    // sensors so the device boots. (SensorDevice already tolerates a
+    // non-functional HAL: every method early-returns.)
+    ALOGE("ISensors HIDL service failure (continuing without sensors): %s.",
+          detail.c_str());
 }
 
 // ---------------------------------------------------------------------------
diff --git a/services/surfaceflinger/DisplayHardware/FramebufferSurface.cpp b/services/surfaceflinger/DisplayHardware/FramebufferSurface.cpp
index e6d783434..68324cfb8 100644
--- a/services/surfaceflinger/DisplayHardware/FramebufferSurface.cpp
+++ b/services/surfaceflinger/DisplayHardware/FramebufferSurface.cpp
@@ -161,8 +161,29 @@ void FramebufferSurface::freeBufferLocked(int slotIndex) {
 
 void FramebufferSurface::onFrameCommitted() {
     if (mHasPendingRelease) {
+        // nicki/msm8960: on a HWC1-via-HWC2On1Adapter that reports
+        // PresentFenceIsNotReliable, the present fence never reliably signals.
+        // Attaching it here as THIS framebuffer buffer's release fence means the
+        // buffer is never freed for reuse, so SF's own next FB dequeue/render
+        // blocks in sync_fence_wait ~10s (SF stuck in msm_fb_pan_idle /
+        // adreno_drawctxt_wait) -- which cascades to EVERY producer and
+        // hard-freezes the UI during any sustained composition (spinner popup,
+        // notification shade, list scroll; DequeueBufferDuration=9.9s). Skip the
+        // unreliable fence so the FB buffer is released immediately (NO_FENCE).
+        // NOTE (2026-07-04): honouring the fence here was retried after keeping HW
+        // vsync permanently enabled (which makes the present fence's --latency
+        // timestamps advance) -- but it STILL froze SystemUI under sustained
+        // composition, so the present fence is genuinely unusable as an FB release
+        // gate on this device, not merely a vsync-armed artifact. Keep NO_FENCE.
+        // This is the SF-output sibling of the disable_backpressure (frameMissed)
+        // and postFramebuffer per-layer release-fence fixes -- same unreliable-
+        // present-fence root cause (task #28). The vendor HWC's blocking FB pan
+        // (msm_fb_pan_idle) already serializes access on this single display, so
+        // dropping the redundant fence is safe; steady-state (non-cycling)
+        // surfaces never hit it, which is why the static keyguard hid it.
         sp<Fence> fence = mHwc.getPresentFence(mDisplayType);
-        if (fence->isValid()) {
+        if (fence->isValid() &&
+                !mHwc.hasCapability(HWC2::Capability::PresentFenceIsNotReliable)) {
             status_t result = addReleaseFence(mPreviousBufferSlot,
                     mPreviousBuffer, fence);
             ALOGE_IF(result != NO_ERROR, "onFrameCommitted: failed to add the"
diff --git a/services/surfaceflinger/SurfaceFlinger.cpp b/services/surfaceflinger/SurfaceFlinger.cpp
index f513b9fd7..220714111 100644
--- a/services/surfaceflinger/SurfaceFlinger.cpp
+++ b/services/surfaceflinger/SurfaceFlinger.cpp
@@ -1428,7 +1428,13 @@ void SurfaceFlinger::onVsyncReceived(int32_t sequenceId,
         }
     }
 
-    if (needsHwVsync) {
+    // nicki/msm8960: keep HW vsync permanently enabled when the present fence is
+    // unreliable -- addResyncSample() relocks DispSync to the real panel vsync
+    // every refresh, and (critically) it keeps the mdp4 vsync IRQ armed so the
+    // retire fence backing the per-layer release fences actually advances.
+    if (needsHwVsync ||
+            getHwComposer().hasCapability(
+                    HWC2::Capability::PresentFenceIsNotReliable)) {
         enableHardwareVsync();
     } else {
         disableHardwareVsync(false);
@@ -1839,7 +1845,13 @@ void SurfaceFlinger::postComposition(nsecs_t refreshStartTime)
         }
     });
 
-    if (presentFenceTime->isValid()) {
+    // nicki/msm8960: do NOT feed this HWC's unreliable present fence into DispSync
+    // (it corrupts the vsync model and makes SF disableHardwareVsync). Keeping HW
+    // vsync always on (see onVsyncReceived) is also what makes the mdp4 retire
+    // fence advance so the per-layer release fences in postFramebuffer signal.
+    if (presentFenceTime->isValid() &&
+            !getHwComposer().hasCapability(
+                    HWC2::Capability::PresentFenceIsNotReliable)) {
         if (mPrimaryDispSync.addPresentFence(presentFenceTime)) {
             enableHardwareVsync();
         } else {
@@ -2210,6 +2222,24 @@ void SurfaceFlinger::postFramebuffer()
         }
         displayDevice->onSwapBuffersCompleted();
         displayDevice->makeCurrent();
+        // nicki/msm8960: HONOUR the per-layer release fence from
+        // getLayerReleaseFence() on this HWC1-via-HWC2On1Adapter. An earlier
+        // bring-up pass forced NO_FENCE here (immediate release) because on a HWC
+        // that reports PresentFenceIsNotReliable the per-layer release fence
+        // derives from the mdp4 retire fence, which never signalled -- handing it
+        // to the producer made the next dequeueBuffer() block ~10s
+        // (DequeueBufferDuration=9.9s) and hard-froze any animating surface. But
+        // that stall was a *side effect of HW vsync being disabled*: the retire
+        // fence only advances while the panel vsync IRQ is armed. SurfaceFlinger
+        // now keeps HW vsync permanently enabled on this HWC
+        // (onVsyncReceived/postComposition, guarded by PresentFenceIsNotReliable),
+        // so the fence signals ~2-3 refreshes late and the triple-buffered app
+        // producers absorb that latency without starving. Gating the buffer
+        // release on the fence (release only AFTER the MDP has scanned the buffer
+        // out) stops the producer from overwriting a buffer the MDP overlay is
+        // still displaying -- which was the residual per-layer FLICKER. The
+        // CLIENT-composited path below additionally merges the client-target
+        // acquire fence, a real reliable EGL fence.
         for (auto& layer : displayDevice->getVisibleLayersSortedByZ()) {
             sp<Fence> releaseFence = Fence::NO_FENCE;
 
