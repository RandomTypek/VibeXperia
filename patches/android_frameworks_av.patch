diff --git a/camera/Android.bp b/camera/Android.bp
index 58cc622c3..8146fb097 100644
--- a/camera/Android.bp
+++ b/camera/Android.bp
@@ -108,6 +108,12 @@ cc_library_shared {
         "-Werror",
         "-Wall",
         "-Wextra",
+        // nicki: inject the device's Sony CameraParameters extras
+        // (KEY_SONY_*/KEY_GPS_*_REF) so libcamera_client defines the static
+        // members the legacy Sony camera blob (camera.vendor.msm8960.so)
+        // needs to resolve at dlopen. Only CameraParameters.{h,cpp} use the
+        // CAMERA_PARAMETERS_EXTRA_{H,C} macros; harmless for other sources.
+        "-include device/sony/nicki/include/camera/CameraParametersExtra.h",
     ],
 
 }
diff --git a/camera/CameraParameters.cpp b/camera/CameraParameters.cpp
index de8ac2ffc..ec01af0af 100644
--- a/camera/CameraParameters.cpp
+++ b/camera/CameraParameters.cpp
@@ -537,4 +537,8 @@ bool CameraParameters::isEmpty() const {
     return mMap.isEmpty();
 }
 
+#ifdef CAMERA_PARAMETERS_EXTRA_C
+CAMERA_PARAMETERS_EXTRA_C
+#endif
+
 }; // namespace android
diff --git a/camera/include/camera/CameraParameters.h b/camera/include/camera/CameraParameters.h
index ba33ffe63..97b0fddc9 100644
--- a/camera/include/camera/CameraParameters.h
+++ b/camera/include/camera/CameraParameters.h
@@ -690,6 +690,10 @@ public:
      */
     static int previewFormatToEnum(const char* format);
 
+#ifdef CAMERA_PARAMETERS_EXTRA_H
+CAMERA_PARAMETERS_EXTRA_H
+#endif
+
 private:
     DefaultKeyedVector<String8,String8>    mMap;
 };
