diff --git a/camera/device/1.0/default/CameraDevice.cpp b/camera/device/1.0/default/CameraDevice.cpp
index 116963b9..a8b00a47 100644
--- a/camera/device/1.0/default/CameraDevice.cpp
+++ b/camera/device/1.0/default/CameraDevice.cpp
@@ -16,6 +16,7 @@
 
 #define LOG_TAG "CamDev@1.0-impl"
 #include <hardware/camera.h>
+#include <hardware/gralloc.h>
 #include <hardware/gralloc1.h>
 #include <hidlmemory/mapping.h>
 #include <log/log.h>
@@ -36,6 +37,30 @@ using ::android::hardware::graphics::common::V1_0::PixelFormat;
 
 HandleImporter CameraDevice::sHandleImporter;
 
+// nicki: track the single currently-open HAL1 device so sGetMemory can recover
+// the real CameraDevice* when the legacy snapshot stream passes a bogus cookie.
+Mutex CameraDevice::sOpenDeviceLock;
+CameraDevice* CameraDevice::sOpenDevice = nullptr;
+
+// nicki: the legacy HAL1 camera blob (camera.vendor.msm8960.so) reads the preview
+// buffer's mapped CPU address straight out of the QC private_handle_t (->base); it
+// imports no gralloc/mapper symbols of its own. Oreo's gralloc mapper imports
+// lazily (no mmap until lock), so after HandleImporter::importBuffer ->base is
+// still 0 -> the blob null-derefs in QCameraStream_preview::initDisplayBuffers().
+// Force the mmap by locking each freshly-imported buffer through the SAME legacy
+// gralloc0 module that allocated it, which populates ->base.
+static const gralloc_module_t* nickiGrallocModule() {
+    static const gralloc_module_t* sMod = []() -> const gralloc_module_t* {
+        const hw_module_t* mod = nullptr;
+        if (hw_get_module(GRALLOC_HARDWARE_MODULE_ID, &mod) != 0 || mod == nullptr) {
+            ALOGE("nicki: hw_get_module(gralloc) failed; preview buffers won't be mapped");
+            return nullptr;
+        }
+        return reinterpret_cast<const gralloc_module_t*>(mod);
+    }();
+    return sMod;
+}
+
 Status CameraDevice::getHidlStatus(const int& status) {
     switch (status) {
         case 0: return Status::OK;
@@ -137,7 +162,12 @@ void CameraDevice::setConnectionStatus(bool connected) {
 
 void CameraDevice::CameraPreviewWindow::cleanUpCirculatingBuffers() {
     Mutex::Autolock _l(mLock);
+    const gralloc_module_t* gr = nickiGrallocModule();
     for (auto pair : mCirculatingBuffers) {
+        // nicki: balance the gralloc lock taken in sDequeueBuffer before freeing.
+        if (gr != nullptr && gr->unlock != nullptr) {
+            gr->unlock(gr, pair.second);
+        }
         sHandleImporter.freeBuffer(pair.second);
     }
     mCirculatingBuffers.clear();
@@ -171,6 +201,27 @@ int CameraDevice::sDequeueBuffer(struct preview_stream_ops* w,
                         s = Status::INTERNAL_ERROR;
                         return;
                     } else {
+                        // nicki: force-map the buffer so the legacy blob sees a
+                        // non-null ->base. Oreo's mapper imports lazily, leaving
+                        // the QC private_handle unmapped -> blob null-derefs in
+                        // initDisplayBuffers. A gralloc0 lock mmaps it and writes
+                        // ->base. Keep it locked for the buffer's lifetime (the
+                        // blob reads ->base on every frame); unlocked on free.
+                        const gralloc_module_t* gr = nickiGrallocModule();
+                        if (gr != nullptr && gr->lock != nullptr) {
+                            void* vaddr = nullptr;
+                            int gw = object->mPreviewWidth ? (int)object->mPreviewWidth : 1;
+                            int gh = object->mPreviewHeight ? (int)object->mPreviewHeight : 1;
+                            int lerr = gr->lock(gr, importedBuf,
+                                    GRALLOC_USAGE_SW_READ_OFTEN |
+                                    GRALLOC_USAGE_SW_WRITE_OFTEN |
+                                    GRALLOC_USAGE_HW_CAMERA_WRITE,
+                                    0, 0, gw, gh, &vaddr);
+                            if (lerr != 0 || vaddr == nullptr) {
+                                ALOGW("%s: gralloc lock to map preview buffer failed"
+                                      " (err=%d vaddr=%p)", __FUNCTION__, lerr, vaddr);
+                            }
+                        }
                         object->mCirculatingBuffers[bufferId] = importedBuf;
                         object->mBufferIdMap[&(object->mCirculatingBuffers[bufferId])] = bufferId;
                     }
@@ -226,6 +277,9 @@ int CameraDevice::sSetBuffersGeometry(struct preview_stream_ops* w,
     }
 
     object->cleanUpCirculatingBuffers();
+    // nicki: remember geometry so dequeued buffers can be gralloc-locked (mapped).
+    object->mPreviewWidth = (width > 0) ? (uint32_t)width : 0;
+    object->mPreviewHeight = (height > 0) ? (uint32_t)height : 0;
     return getStatusT(
             object->mPreviewCallback->setBuffersGeometry(width, height, (PixelFormat) format));
 }
@@ -259,7 +313,14 @@ int CameraDevice::sSetUsage(struct preview_stream_ops* w, int usage) {
     }
 
     object->cleanUpCirculatingBuffers();
-    return getStatusT(object->mPreviewCallback->setUsage((BufferUsage)usage));
+    // nicki: the legacy HAL1 camera blob passes 32-bit gralloc usage flags as a
+    // signed int (e.g. 0xc0020900, high bit set). Casting that straight to the
+    // 64-bit BufferUsage enum sign-extends it to 0xffffffffc0020900, and Oreo's
+    // gralloc mapper rejects the bogus upper 32 bits ("invalid usage bits
+    // 0xffffffff00000000") -> preview buffer allocation fails -> no live preview.
+    // Zero-extend via uint32_t so only the real low 32 usage bits survive.
+    return getStatusT(object->mPreviewCallback->setUsage(
+            (BufferUsage)(uint32_t)usage));
 }
 
 int CameraDevice::sSetSwapInterval(struct preview_stream_ops *w, int interval) {
@@ -362,7 +423,20 @@ CameraDevice::CameraHeapMemory::~CameraHeapMemory() {
 camera_memory_t* CameraDevice::sGetMemory(int fd, size_t buf_size, uint_t num_bufs, void *user) {
     ALOGV("%s", __FUNCTION__);
     CameraDevice* object = static_cast<CameraDevice*>(user);
-    if (object->mDeviceCallback == nullptr) {
+    // nicki: the legacy snapshot stream passes a bogus cookie here (not the
+    // CameraDevice* we handed to set_callbacks), so dereferencing `object`
+    // SIGSEGVs (sGetMemory+157, fault 0x500). Validate the cookie against the
+    // single currently-open device and recover when it doesn't match. Only one
+    // HAL1 camera is open at a time, so sOpenDevice is unambiguous.
+    {
+        Mutex::Autolock _sl(sOpenDeviceLock);
+        if (object != sOpenDevice) {
+            ALOGW("%s: HAL passed cookie %p != open device %p; recovering",
+                    __FUNCTION__, object, sOpenDevice);
+            object = sOpenDevice;
+        }
+    }
+    if (object == nullptr || object->mDeviceCallback == nullptr) {
         ALOGE("%s: camera HAL request memory while camera is not opened!", __FUNCTION__);
         return nullptr;
     }
@@ -375,10 +449,32 @@ camera_memory_t* CameraDevice::sGetMemory(int fd, size_t buf_size, uint_t num_bu
     }
     mem->incStrong(mem);
     hidl_handle hidlHandle = mem->mHidlHandle;
-    MemoryId id = object->mDeviceCallback->registerMemory(hidlHandle, buf_size, num_bufs);
+    // nicki: registerMemory's HIDL round-trip to the client (CameraHardwareInterface)
+    // fails transport for the legacy HAL1 path. For preview DISPLAY buffers the
+    // locally-mmapped CameraHeapMemory below is everything the blob needs (frames
+    // reach the Surface via enqueue_buffer, not via dataCallback), so don't let a
+    // failed registerMemory abort the whole preview. Fall back to a local id.
+    MemoryId id;
+    bool registered;
+    auto ret = object->mDeviceCallback->registerMemory(hidlHandle, buf_size, num_bufs);
+    if (ret.isOk()) {
+        id = ret;
+        registered = true;
+    } else {
+        static uint32_t sLocalMemId = 0x40000000u;
+        id = sLocalMemId++;
+        registered = false;
+        ALOGW("%s: registerMemory transport failed (fd=%d size=%zu num=%u): %s;"
+              " local id %u (not tracked)", __FUNCTION__, fd, buf_size, num_bufs,
+                ret.description().c_str(), id);
+    }
     mem->handle.mId = id;
 
-    {
+    // nicki: only track in mMemoryMap when the client actually registered it.
+    // The map is consulted by the data-callback path (sDataCb) which display
+    // buffers never hit, so skipping it for the local-id fallback is safe and
+    // avoids touching the map for buffers the client doesn't know about.
+    if (registered) {
         Mutex::Autolock _l(object->mMemoryMapLock);
         if (object->mMemoryMap.count(id) != 0) {
             ALOGE("%s: duplicate MemoryId %d returned by client!", __FUNCTION__, id);
@@ -401,8 +497,11 @@ void CameraDevice::sPutMemory(camera_memory_t *data) {
     if (device->mDeviceCallback == nullptr) {
         ALOGE("%s: camera HAL return memory while camera is not opened!", __FUNCTION__);
     }
-    device->mDeviceCallback->unregisterMemory(mem->handle.mId);
-    {
+    // nicki: local-id buffers (>= 0x40000000) were never registered with the
+    // client nor put in mMemoryMap (see sGetMemory), so skip the client
+    // round-trip and map erase for them.
+    if (mem->handle.mId < 0x40000000u) {
+        device->mDeviceCallback->unregisterMemory(mem->handle.mId);
         Mutex::Autolock _l(device->mMemoryMapLock);
         device->mMemoryMap.erase(mem->handle.mId);
     }
@@ -721,6 +820,12 @@ Return<Status> CameraDevice::open(const sp<ICameraDeviceCallback>& callback) {
                 sNotifyCb, sDataCb, sDataCbTimestamp, sGetMemory, this);
     }
 
+    // nicki: register as the single currently-open HAL1 device (see sGetMemory).
+    {
+        Mutex::Autolock _sl(sOpenDeviceLock);
+        sOpenDevice = this;
+    }
+
     return getHidlStatus(rc);
 }
 
@@ -1053,6 +1158,13 @@ Return<void> CameraDevice::close() {
 
 void CameraDevice::closeLocked() {
     ALOGI("Closing camera %s", mCameraId.c_str());
+    // nicki: stop being the recovery target for sGetMemory once closed.
+    {
+        Mutex::Autolock _sl(sOpenDeviceLock);
+        if (sOpenDevice == this) {
+            sOpenDevice = nullptr;
+        }
+    }
     if(mDevice) {
         int rc = mDevice->common.close(&mDevice->common);
         if (rc != OK) {
diff --git a/camera/device/1.0/default/CameraDevice_1_0.h b/camera/device/1.0/default/CameraDevice_1_0.h
index 2b35a730..f53133f6 100644
--- a/camera/device/1.0/default/CameraDevice_1_0.h
+++ b/camera/device/1.0/default/CameraDevice_1_0.h
@@ -156,6 +156,10 @@ private:
         sp<ICameraDevicePreviewCallback> mPreviewCallback = nullptr;
         std::unordered_map<uint64_t, buffer_handle_t> mCirculatingBuffers;
         std::unordered_map<buffer_handle_t*, uint64_t> mBufferIdMap;
+        // nicki: preview geometry cached from set_buffers_geometry, used to
+        // gralloc-lock dequeued buffers so the legacy blob sees a mapped ->base.
+        uint32_t mPreviewWidth = 0;
+        uint32_t mPreviewHeight = 0;
     } mHalPreviewWindow;
 
     // gating access to mDevice, mInitFail, mDisconnected
@@ -167,6 +171,16 @@ private:
 
     static HandleImporter sHandleImporter;
 
+    // nicki: the legacy HAL1 snapshot stream (QCameraStream_SnapshotMain) calls
+    // get_memory with a BOGUS cookie (not the CameraDevice* we passed to
+    // set_callbacks) -> static_cast<CameraDevice*>(user) in sGetMemory derefs
+    // garbage and SIGSEGVs. Only ONE HAL1 camera is ever open at a time
+    // (CameraClient is strictly single-open), so we track the single currently-
+    // open instance and recover the real device from it whenever the cookie
+    // handed back doesn't match. Set in open(), cleared in closeLocked().
+    static Mutex sOpenDeviceLock;
+    static CameraDevice* sOpenDevice;
+
     const SortedVector<std::pair<std::string, std::string>>& mCameraDeviceNames;
 
     sp<ICameraDeviceCallback> mDeviceCallback = nullptr;
diff --git a/camera/provider/2.4/default/android.hardware.camera.provider@2.4-service.rc b/camera/provider/2.4/default/android.hardware.camera.provider@2.4-service.rc
index 2bf309b4..b1996156 100644
--- a/camera/provider/2.4/default/android.hardware.camera.provider@2.4-service.rc
+++ b/camera/provider/2.4/default/android.hardware.camera.provider@2.4-service.rc
@@ -5,3 +5,10 @@ service camera-provider-2-4 /vendor/bin/hw/android.hardware.camera.provider@2.4-
     ioprio rt 4
     capabilities SYS_NICE
     writepid /dev/cpuset/camera-daemon/tasks /dev/stune/top-app/tasks
+    # nicki: treble is OFF, so the camera HAL1 must run passthrough inside
+    # cameraserver/mediaserver (a system process). This binderized provider runs
+    # as a vendor process that cannot reach the system 'sensorservice', so
+    # camera.qcom.so's camera_device_open -> SensorManager::assertStateLocked()
+    # LOG_ALWAYS_FATAL-aborts, enumerating 0 cameras. Disabled so the framework
+    # falls back to the passthrough @2.4-impl (which enumerates + opens both cams).
+    disabled
diff --git a/sensors/1.0/default/Sensors.cpp b/sensors/1.0/default/Sensors.cpp
index 1100dd6a..26519b75 100644
--- a/sensors/1.0/default/Sensors.cpp
+++ b/sensors/1.0/default/Sensors.cpp
@@ -93,9 +93,21 @@ Sensors::Sensors()
         return;
     }
 
-    // Require all the old HAL APIs to be present except for injection, which
-    // is considered optional.
-    CHECK_GE(getHalDeviceVersion(), SENSORS_DEVICE_API_VERSION_1_3);
+    // nicki's 2012 Bosch (bsthal) sensors HAL is ABI-incompatible with Oreo:
+    // version 0 + old, smaller sensor_t layout. We can't enumerate it safely
+    // (garbage handles -> SIGSEGV in SensorService). But returning an error
+    // here (mInitCheck != OK) makes the binderized sensors-hal-1-0 service fail
+    // to register and crash-loop. Instead initialize OK so the service stays
+    // up, and report ZERO sensors from getSensorsList() (without touching the
+    // blob) so SensorService has nothing to enumerate. Sensors are simply
+    // unavailable until the HAL is brought up with a compatible sensor_t.
+    if (getHalDeviceVersion() < SENSORS_DEVICE_API_VERSION_1_3) {
+        LOG(WARNING) << "Sensors HAL device version 0x" << std::hex
+                     << getHalDeviceVersion() << std::dec
+                     << " is too old/ABI-incompatible for Oreo; reporting no "
+                        "sensors (HAL stays registered, system boots).";
+        // fall through: mInitCheck = OK below.
+    }
 
     if (getHalDeviceVersion() == SENSORS_DEVICE_API_VERSION_1_4) {
         if (mSensorDevice->inject_sensor_data == nullptr) {
@@ -114,6 +126,18 @@ status_t Sensors::initCheck() const {
 }
 
 Return<void> Sensors::getSensorsList(getSensorsList_cb _hidl_cb) {
+    // nicki's 2012 Bosch (bsthal) HAL returns the OLD, smaller sensor_t layout.
+    // Oreo's wrapper strides this list with the newer/larger sensor_t, misreads
+    // every field (garbage handles like 0x5F41xxxx) and SIGSEGVs system_server.
+    // Report ZERO sensors WITHOUT calling get_sensors_list() (touching the blob
+    // crashes too), so SensorService has nothing to enumerate and the device
+    // boots. Sensors are unavailable until the HAL has a compatible sensor_t.
+    if (getHalDeviceVersion() < SENSORS_DEVICE_API_VERSION_1_3) {
+        hidl_vec<SensorInfo> none;
+        _hidl_cb(none);
+        return Void();
+    }
+
     sensor_t const *list;
     size_t count = mSensorModule->get_sensors_list(mSensorModule, &list);
 
@@ -235,6 +259,18 @@ Return<Result> Sensors::batch(
         int32_t sensor_handle,
         int64_t sampling_period_ns,
         int64_t max_report_latency_ns) {
+    // batch() was added in HAL 1.1. nicki's legacy bsthal reports version 0,
+    // so the sensors_poll_device_1 fields beyond the 0.x layout (->batch) are
+    // invalid memory; calling them null/garbage-derefs and crashes
+    // system_server. Fall back to the 0.x setDelay() and report success.
+    if (getHalDeviceVersion() < SENSORS_DEVICE_API_VERSION_1_1) {
+        if (mSensorDevice->setDelay != nullptr) {
+            mSensorDevice->setDelay(
+                    reinterpret_cast<sensors_poll_device_t *>(mSensorDevice),
+                    sensor_handle, sampling_period_ns);
+        }
+        return Result::OK;
+    }
     return ResultFromStatus(
             mSensorDevice->batch(
                 mSensorDevice,
@@ -245,6 +281,10 @@ Return<Result> Sensors::batch(
 }
 
 Return<Result> Sensors::flush(int32_t sensor_handle) {
+    // flush() was added in HAL 1.3; legacy HALs don't implement it.
+    if (getHalDeviceVersion() < SENSORS_DEVICE_API_VERSION_1_3) {
+        return Result::BAD_VALUE;
+    }
     return ResultFromStatus(mSensorDevice->flush(mSensorDevice, sensor_handle));
 }
 
diff --git a/sensors/1.0/default/android.hardware.sensors@1.0-service.rc b/sensors/1.0/default/android.hardware.sensors@1.0-service.rc
index 6e78082b..60d3af63 100644
--- a/sensors/1.0/default/android.hardware.sensors@1.0-service.rc
+++ b/sensors/1.0/default/android.hardware.sensors@1.0-service.rc
@@ -1,5 +1,11 @@
 service sensors-hal-1-0 /vendor/bin/hw/android.hardware.sensors@1.0-service
     class hal
     user system
-    group system wakelock
+    # nicki: the legacy bsthal sub-HAL reads sensor data straight from
+    # /dev/input/event* (bma250/bmm050/lightsensor/proximity), which are
+    # crw-rw---- root:input. Without the 'input' group the service can read
+    # config via sysfs (so activate/setDelay work) but cannot open the event
+    # nodes -> "couldn't find 'bma250' input device" and ZERO events reach
+    # SensorService (auto-rotate / apps see no values). Add 'input'.
+    group system wakelock input
     capabilities BLOCK_SUSPEND SYS_NICE
